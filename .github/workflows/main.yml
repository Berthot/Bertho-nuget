name: Build and deploy MeuPacote Package

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@main

    - name: Set up .NET Core
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '8.0.x'
        source-url: https://nuget.pkg.github.com/yourusername/index.json
      env:
        NUGET_AUTH_TOKEN: ${{secrets.NUGET_PACKAGE_TOKEN}}

    - name: Build with dotnet
      run: dotnet build --configuration Release

    - name: Create the Package
      run: dotnet pack --configuration Release

    - name: Publish
      run: dotnet nuget push "bin/Release/*.nupkg" -k ${{ secrets.NUGET_PACKAGE_TOKEN }} -s https://nuget.pkg.github.com/yourusername/index.json --skip-duplicate
